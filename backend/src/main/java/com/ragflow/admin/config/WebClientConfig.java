package com.ragflow.admin.config;

import org.springframework.context.annotation.Configuration;

/**
 * WebClient is now managed dynamically by RagflowConfigService
 * based on the RAGFlow URL stored in sys_config table.
 * This config class is kept as a placeholder.
 */
@Configuration
public class WebClientConfig {
}
